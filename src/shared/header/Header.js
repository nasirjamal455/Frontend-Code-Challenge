import React from 'react';
import Navbar from '../../bootstrap/components/navbar/Navbar';

const Header = () => {
    return (
        <React.Fragment>

        <Navbar />
        </React.Fragment>
    );
}

export default Header;
